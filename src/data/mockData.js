// All mock data has been unmocked and migrated directly to Neon PostgreSQL.
// All datasets (schools, users, live_classes, curriculum, assignments, submissions, attendance)
// are dynamically queried via backend REST endpoints (/api/*).
export const UNMOCKED = true;
